<article>
  <slot />
</article>
<style>
  article {
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: var(--gap);
    padding: var(--padding);
    border-radius: var(--rounded);
    background-color: var(--gray-ultra-light);
  }
</style>
